# Local credentials — NEVER commit this
.env

# Node modules — recreated with npm install
node_modules/

# macOS junk
.DS_Store

# Editor folders
.vscode/
.idea/
.claude/
settings.json
dist/

# Vite build output — generated by `npm run build`, do not hand-edit
public/

# Component library build output — generated by `npm run build:lib`
dist-ui/

# design-sync: staged converter scripts + isolated deps, generated bundle,
# machine verification state — regenerated by re-running the sync
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
